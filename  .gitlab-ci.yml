stages:
  - trigger
  - deploy

trigger-Doctors:
  stage: trigger
  trigger:
    include: Doctors/.gitlab-ci.yml
    strategy: depend
  rules:
    - changes:
        - Doctors/**/*

deploy:
  stage: deploy
  tags:
    - all
  only:
    - CI/CD_dev
  script:
    - docker-compose -f dockerize/docker-compose.yaml pull
    - docker-compose up -d
    - docker-compose down
